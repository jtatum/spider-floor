{
  "name": "the-worst-elevator",
  "version": "0.1.0",
  "private": true,
  "description": "An elevator roguelike — operate a junk lift with no floor display.",
  "scripts": {
    "test": "node tests/run.mjs"
  }
}
